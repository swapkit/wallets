pre-commit:
  commands:
    lint:
      glob: "*.{jsx,tsx,ts,js,json}"
      run: bun biome check --write --no-errors-on-unmatched {staged_files} && git add {staged_files}
    type-check:
      glob: "*.{ts,tsx}"
      run: bun type-check
